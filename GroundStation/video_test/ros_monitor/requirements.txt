# Runtime deps for the wildbridge_controller node + monitor (ROS packages come
# from the base image). requests comes from the shared wildbridge-groundstation
# package, which this image also installs.
numpy>=1.21
